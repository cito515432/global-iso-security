# Alternativa rápida: usa un .jar ya compilado dentro de backend/target/.
# Úsalo solo si no quieres que Docker compile con Maven.

FROM eclipse-temurin:21-jre
WORKDIR /app

ENV PORT=8081

COPY target/backend-0.0.1-SNAPSHOT.jar app.jar

EXPOSE 8081
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
